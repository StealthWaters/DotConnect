# -*- mode: python ; coding: utf-8 -*-


a = Analysis(
    ['DotConnect.py'],
    pathex=[],
    binaries=[],
    datas=[('images', 'images'), ('C:\\Users\\Steal\\AppData\\Local\\Programs\\Python\\Python313\\Lib\\site-packages\\pgzero\\data\\icon.png', 'pgzero\\data')],
    hiddenimports=[],
    hookspath=[],
    hooksconfig={},
    runtime_hooks=[],
    excludes=[],
    noarchive=False,
    optimize=0,
)
pyz = PYZ(a.pure)

exe = EXE(
    pyz,
    a.scripts,
    [],
    exclude_binaries=True,
    name='DotConnect',
    debug=False,
    bootloader_ignore_signals=False,
    strip=False,
    upx=True,
    console=True,
    disable_windowed_traceback=False,
    argv_emulation=False,
    target_arch=None,
    codesign_identity=None,
    entitlements_file=None,
    version='version.txt',
    icon=['icon.ico'],
)
coll = COLLECT(
    exe,
    a.binaries,
    a.datas,
    strip=False,
    upx=True,
    upx_exclude=[],
    name='DotConnect',
)
